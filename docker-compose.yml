newscoop:
    extends:
        file: docker/docker-newscoop.yml
        service: newscoop
    links:
        - mysql

mysql:
    extends:
        file: docker/docker-newscoop.yml
        service: mysql

