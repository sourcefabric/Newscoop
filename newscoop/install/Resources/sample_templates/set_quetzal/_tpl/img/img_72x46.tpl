{{ image rendition="slidermostpopular" }}
<img class="pull-left" src="{{ $image->src }}" width="{{ $image->width }}" height="{{ $image->height }}" alt="{{ $image->photographer}}: {{ $image->caption }}" />
{{ /image }}
